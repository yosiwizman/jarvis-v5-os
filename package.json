{
  "name": "jarvis",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "node apps/web/scripts/mkcert-dev.mjs && concurrently -n server,next,tls \"npm -w @jarvis/server run dev\" \"cd apps/web && npx next dev -p 3001 -H localhost\" \"cd apps/web && node dev-proxy.mjs\"",
    "start:ci": "concurrently -k -s first -n server,next \"npm -w @jarvis/server run dev\" \"cd apps/web && npx next dev -p 3000 -H 0.0.0.0\"",
    "web": "node apps/web/scripts/mkcert-dev.mjs && concurrently -n next,tls \"cd apps/web && npx next dev -p 3001 -H localhost\" \"cd apps/web && node dev-proxy.mjs\"",
    "dev": "npm start",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "test:settings": "tsx scripts/test-settings-normalization.ts",
    "test:contract": "tsx scripts/test-settings-contract.ts",
    "test:notifications": "tsx scripts/test-notifications-sse.ts",
    "test:notifications:ci": "cross-env BACKEND_URL=http://localhost:1234 tsx scripts/test-notifications-sse.ts",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --project=chromium",
    "smoke": "tsx scripts/smoke.ts",
"smoke:ci": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 JARVIS_SMOKE_BASE_URL=http://localhost:3000 JARVIS_SMOKE_API_URL=https://localhost:1234 tsx scripts/smoke.ts",
"ci:smoke": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 concurrently -k -s first \"npm run start:ci\" \"wait-on -t 120000 tcp:localhost:3000 tcp:localhost:1234 && sleep 5 && npm run smoke:ci\"",
    "clean": "rimraf node_modules apps/*/node_modules packages/*/node_modules .next apps/*/.next"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1",
    "turbo": "latest",
    "wait-on": "^7.2.0"
  }
}
